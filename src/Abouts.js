import { View, Button, StyleSheet,useState, Linking, Text, TouchableOpacity, TextInput, onChangeText } from 'react-native'
import React from 'react'

const Abouts = () => {
  return (
    <View style={{backgroundColor:'red', height:500}}> 
      <Text style={{color:'white',fontWeight:50, marginVertical:200, marginHorizontal:100,}} >Welcome</Text>
    </View>
  )
}

export default Abouts

const styles = StyleSheet.create({})